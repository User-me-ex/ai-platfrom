{
  "name": "antigravity-models",
  "displayName": "9 Router Models",
  "description": "Pick AI models served by the local 9 Router gateway and chat with them — streaming replies and agentic file creation.",
  "version": "0.5.20",
  "publisher": "antigravity-dev",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/User-me-ex/ai-platfrom.git"
  },
  "activationEvents": [
    "onCommand:antigravity.models.click",
    "onCommand:antigravity.models.pick",
    "onCommand:antigravity.models.chat",
    "onCommand:antigravity.models.options",
    "onCommand:antigravity.models.voice",
    "onCommand:antigravity.voice.talk",
    "onCommand:antigravity.voice.mute",
    "onCommand:antigravity.voice.devices",
    "onStartupFinished"
  ],
  "main": "./out/src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "antigravity.models.click",
        "title": "Open (Chat / Pick Model)",
        "category": "9 Router"
      },
      {
        "command": "antigravity.models.pick",
        "title": "Pick Model",
        "category": "9 Router"
      },
      {
        "command": "antigravity.models.chat",
        "title": "Chat with Model",
        "category": "9 Router"
      },
      {
        "command": "antigravity.models.options",
        "title": "Session Options",
        "category": "9 Router"
      },
      {
        "command": "antigravity.models.voice",
        "title": "Voice Conversation",
        "category": "9 Router"
      },
      {
        "command": "antigravity.voice.talk",
        "title": "Voice: Talk / Stop talking",
        "category": "9 Router"
      },
      {
        "command": "antigravity.voice.mute",
        "title": "Voice: Mute / Unmute",
        "category": "9 Router"
      },
      {
        "command": "antigravity.voice.devices",
        "title": "Voice: List audio devices",
        "category": "9 Router"
      },
      {
        "command": "antigravity.voice.micLevel",
        "title": "Voice: Check mic level",
        "category": "9 Router"
      }
    ],
    "configuration": {
      "title": "9 Router Models",
      "properties": {
        "antigravity.router.baseUrl": {
          "type": "string",
          "default": "http://127.0.0.1:20128/v1",
          "scope": "window",
          "description": "9 Router base URL (OpenAI-compatible). Defaults to http://127.0.0.1:20128/v1"
        },
        "antigravity.router.apiKey": {
          "type": "string",
          "default": "",
          "scope": "window",
          "description": "Optional API key for the 9 Router. Leave empty if auth is disabled."
        },
        "antigravity.models.filterLive": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "description": "Hide live/voice conversation (voice-to-voice) models from the model picker. Turn off to browse and select them."
        },
        "antigravity.session.temperature": {
          "type": "number",
          "default": 0.5,
          "minimum": 0,
          "maximum": 2,
          "scope": "window",
          "description": "Sampling temperature for chat replies (0 = strict, 1 = creative)."
        },
        "antigravity.session.maxTokens": {
          "type": "number",
          "default": 4096,
          "minimum": 1,
          "maximum": 131072,
          "scope": "window",
          "description": "Maximum tokens the model may produce per reply."
        },
        "antigravity.session.systemPrompt": {
          "type": "string",
          "default": "",
          "scope": "window",
          "description": "Extra system instructions prepended to the agent rules."
        },
        "antigravity.session.allowShell": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "description": "Allow the agent to run shell commands."
        },
        "antigravity.session.allowVscode": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "description": "Allow the agent to run VS Code commands."
        },
        "antigravity.session.allowFiles": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "description": "Allow the agent to read, list, open and write files."
        },
        "antigravity.session.maxTurns": {
          "type": "number",
          "default": 40,
          "minimum": 2,
          "scope": "window",
          "description": "Conversation history length (messages beyond this get trimmed)."
        },
        "antigravity.voice.liveModel": {
          "type": "string",
          "default": "gemini-3.1-flash-live-preview",
          "scope": "window",
          "description": "Gemini Live model id used by the real-time voice channel (audio-in / audio-out over WebSocket). Matches a 9 Router Gemini id."
        },
        "antigravity.voice.routerPort": {
          "type": "number",
          "default": 20128,
          "scope": "machine",
          "description": "9 Router port that serves the voice WebSocket channel (ws://127.0.0.1:<port>/voice). The channel uses the API key of 9 Router's active gemini/vertex provider connection."
        },
        "antigravity.voice.inputDevice": {
          "type": "string",
          "default": "",
          "scope": "window",
          "description": "Microphone name(s) for sox, e.g. \"Microphone Array\", or a JSON array of preferred names in order: [\"Microphone Array\",\"Microphone (Realtek Audio)\"]. Empty or \"#auto\" (default) = auto-detect the real mic on any machine (never picks a Stereo Mix/loopback). Use \"Voice: List audio devices\" to see the exact names this PC exposes."
        },
        "antigravity.voice.echoCancellation": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "description": "Acoustic echo cancellation (AEC3, the same algorithm browsers use, running in-process as WASM). Cancels the AI's own voice picked up by the mic before it reaches Gemini — removes the 'AI repeats its own last words' problem. Disable to send raw mic audio exactly like the legacy build."
        },
        "antigravity.voice.echoDelayMs": {
          "type": "number",
          "default": 120,
          "scope": "window",
          "description": "Render-to-capture echo-path latency the AEC must compensate for (ms). The AI's voice takes time to leave the speakers and reach the mic; telling the AEC this delay lets it subtract the echo instead of gating your voice. 100-200 is the typical working range; if echo returns, raise it; if your voice is muted while the AI talks (and you're NOT interrupting), lower it."
        },
        "antigravity.voice.ttsVoice": {
          "type": "string",
          "default": "Kore",
          "scope": "window",
          "description": "Voice name for Gemini Live replies (e.g. Kore, Puck, Charon, Aoede)."
        },
        "antigravity.voice.soxPath": {
          "type": "string",
          "default": "",
          "scope": "window",
          "description": "Path to sox.exe for mic capture. Leave empty to auto-detect."
        },
        "antigravity.voice.ffplayPath": {
          "type": "string",
          "default": "",
          "scope": "window",
          "description": "Path to ffplay.exe for audio playback. Leave empty to auto-detect."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "vscode:prepublish": "tsc -p ./"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/vscode": "^1.90.0",
    "@types/ws": "^8.5.13",
    "@vscode/vsce": "^3.2.1",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@ennuicastr/webrtcaec3.js": "^0.3.0",
    "ws": "^8.18.0"
  }
}
